import * as React from "react";
import * as VisuallyHiddenPrimitive from "@radix-ui/react-visually-hidden@1.1.2";

export const VisuallyHidden = VisuallyHiddenPrimitive.Root;

export default VisuallyHidden;
